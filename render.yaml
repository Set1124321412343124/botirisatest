services:
  - type: web
    name: botirisatest
    runtime: python
    buildCommand: |
      export CARGO_HOME=$HOME/.cargo
      pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0   # Use a stable version with pre‑built wheels
